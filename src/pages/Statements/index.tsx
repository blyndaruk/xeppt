import React, { FC } from 'react';

const StatementsPage: FC = () => {
  return (
    <div
      style={{
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        minHeight: '100vh',
        fontSize: 40,
        padding: 40,
        fontWeight: 'bold',
      }}
    >
      Statements
    </div>
  );
};

export default StatementsPage;
